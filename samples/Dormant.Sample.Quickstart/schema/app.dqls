module app;

entity User {
  id: uuid primary;
  email: str;
  created_at: datetime;
  bio: str?;
  posts: multi Post;
  version: int concurrency;
}

entity Post {
  id: uuid primary;
  title: str;
  author: User;
}
