module app;

entity User {
  id: uuid primary;
  email: str;
  created_at: datetime;
  bio: str?;
  posts: Set<Post>;
  version: int concurrency;
}

entity Post {
  id: uuid primary;
  title: str;
  author: User;
}
