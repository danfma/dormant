module app;

entity User {
  id: uuid { constraint primary; }
  email: str;
  created_at: datetime;
  bio: str?;
  posts: Set<Post>;
  version: int { constraint concurrency; }
}

entity Post {
  id: uuid { constraint primary; }
  title: str;
  author: User;
}
