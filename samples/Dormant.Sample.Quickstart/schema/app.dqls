module app;

entity User {
  id: Uuid { constraint primary; }
  email: String;
  created_at: DateTime;
  bio: String?;
  posts: Set<Post>;
  version: Int { constraint concurrency; }
}

entity Post {
  id: Uuid { constraint primary; }
  title: String;
  author: User;
}
