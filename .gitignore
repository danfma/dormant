# .NET
bin/
obj/
*.user
*.suo
*.userprefs
artifacts/
[Pp]ackages/
*.nupkg
*.snupkg

# Build / publish output
publish/
[Dd]ebug/
[Rr]elease/
*.binlog

# Test / coverage
[Tt]est[Rr]esults/
coverage/
*.coverage
*.trx

# BenchmarkDotNet
BenchmarkDotNet.Artifacts/

# IDE / editor
.vs/
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local env
.env
.env.*
!.env.example

# Node.js / editor tooling (Feature 011 — tooling/)
node_modules/
tooling/vscode-dormantql/out/
# Zed clones the grammar repo here when installing the dev extension — local artifact, not ours
tooling/zed-dormantql/grammars/
# Note: dormantql-*.vsix and the generated tree-sitter parser (parser.c, grammar.json,
# node-types.json, tree_sitter/) ARE committed — they are distributable artifacts so
# consumers (Zed, sideload) do not need the tree-sitter CLI to build the grammar.
